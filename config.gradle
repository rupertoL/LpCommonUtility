/**
 * 在主项目的根目录下创建config.gradle文件
 * 在这里单独处理统一依赖问题
 * 注意需要在根目录的build.gradle中进行引入
 */

ext {
    configuration = [

            package          : "com.lp.utlis",
            buildToolsVersion: "28.0.3",
            compileVersion   : 28,
            minSdk           : 17,
            targetSdk        : 27,
            version_code     : 1,
            version_name     : "1.0.0",

    ]


    libraries = [
            supportVersion: "28.0.0"

    ]
}